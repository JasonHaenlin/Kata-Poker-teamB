package poker;

import org.junit.Ignore;
import org.junit.Test;

import static org.junit.Assert.*;

public class GameTest {

    @Ignore
    @Test
    public void buildingDeckTest() {
        //Hand hand = new Hand(1);
       // assertFalse("1", hand.buildNewHand(1)); //Erreur de saisie
        //assertFalse("2", hand.buildNewHand(15)); //Erreur de saisie
        //assertFalse("3", hand.buildNewHand(0)); //Erreur de saisie
        //assertTrue("4", hand.buildNewHand(5));
        //assertTrue("5", hand.buildNewHand(7));
       // assertTrue("6", hand.buildNewHand(14));
    }

}